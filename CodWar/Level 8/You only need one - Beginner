#include <vector>
#include <string>

bool check(const std::vector<std::string>& seq, const std::string& elem) {
  
  int r = 0;
  for(int i = 0; i < seq.size(); i++)
  {
    if(seq[i] == elem)
      r++;
  }
  return r;
}
