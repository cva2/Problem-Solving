#include <string>
using namespace std;

std::string makeUpperCase (const std::string& str)
{
  string res = "";
  for(int i = 0; i < str.length(); i++)
      res += toupper(str[i]);

  return res;
}

