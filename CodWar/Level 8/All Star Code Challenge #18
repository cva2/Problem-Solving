#include <string>

unsigned int strCount(const std::string& word, char letter){ // your code here
  int res = 0;
  for(int i = 0; i < word.length(); i++)
  {
    if(word[i] == letter)
      res++;
  }
  return res;
}
