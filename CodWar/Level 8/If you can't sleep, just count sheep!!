#include <string>
#include <cstring>
using namespace std;

std::string countSheep(int number) {
  string res = "";
  if(number < 1)
    return "";
  
  for(int i = 1; i <= number; i++)
  {
    
    res += to_string(i) + " sheep...";
  }

  return res;
  // your code here
}
